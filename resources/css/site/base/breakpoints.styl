$breakpoints = {
	xs: 25rem, // ~400px
	sm: 40rem, // ~640px
	md: 48rem, // ~768px
	lg: 64rem, // ~1024px
	xl: 80rem,  // ~1280px
	xxl: 90rem  // ~1440px
}

for key, value in $breakpoints
	define(key, 'only screen and (min-width: ' + value + ')')