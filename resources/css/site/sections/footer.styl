body > footer
	.wrapper
		wrapper-x()
		display flex
		flex-direction column
		gap $px-4
		align-items center

		.footer__socials
			display flex
			gap $px-12

		.footer__links
			display flex
			flex-wrap wrap
			justify-content center
			gap $px-8 $px-16