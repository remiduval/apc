.homepage
	flex 1
	display flex
	flex-direction column
	gap $px-160

	.titles
		display flex
		flex-direction column
		gap $px-12

	h2
		font-family $font-display
		fluid(font-size, $text-30, $text-36)

	// .intro
	// 	font-size $text-20